{
	"name": "hono-effect-example",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": "A small REST API where Hono is the web layer and Effect owns the business logic — services, layers, typed errors, and Schema validation.",
	"scripts": {
		"dev": "bun --watch src/server.ts",
		"start": "bun src/server.ts",
		"test": "bun test",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"effect": "4.0.0-beta.86",
		"hono": "^4.6.0"
	},
	"devDependencies": {
		"@types/bun": "^1.3.0",
		"typescript": "^5.9.0"
	}
}
